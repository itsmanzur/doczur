/**
 * `@wordpress/block-editor` ships without bundled type declarations, so the
 * editor-only imports (useBlockProps, InspectorControls) are declared here
 * rather than pulling in an unmaintained @types package.
 */
declare module '@wordpress/block-editor';
